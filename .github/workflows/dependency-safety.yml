name: Dependency Safety

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: {}

concurrency:
  group: dependency-safety-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  safety:
    # Real scan for Dependabot only; every other PR gets the status-only gate
    # in dependency-safety-non-bot-gate.yml. `pull_request.user.login` is the
    # spoofing-resistant actor field.
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write
      statuses: write
      issues: write
    uses: j7an/shared-workflows/.github/workflows/dependency-safety.yml@922a5c02377c33cfb1298fa46f498e71dfad4a70 # v4.8.0
    with:
      auto_merge: true
      release_age_policy: advisory
      minimum_release_age_days: 5
